# ============================================
# HTACCESS - API
# ============================================

# Erlaube nur api.php
<FilesMatch "^(?!api\.php$).*\.php$">
    Require all denied
</FilesMatch>

# Blockiere direkte Aufrufe von Unterordnern
RedirectMatch 403 ^/api/(handlers|helpers)/

# CORS Headers für API (bei Bedarf)
# Aktiviere nur wenn Frontend auf anderer Domain läuft
# Header always set Access-Control-Allow-Origin "*"
# Header always set Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS"
# Header always set Access-Control-Allow-Headers "Content-Type, Authorization, X-API-Key"

# Rate Limiting (optional, wenn mod_ratelimit verfügbar)
# <IfModule mod_ratelimit.c>
#     SetOutputFilter RATE_LIMIT
#     SetEnv rate-limit 400
# </IfModule>